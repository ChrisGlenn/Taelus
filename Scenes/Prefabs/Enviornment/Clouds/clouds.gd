extends CanvasLayer
# CLOUDS
# scrolling clouds that display an overcast effect
# the clouds will fade in (once the sky is visible), and then fade out before it starts to get dark
# if it's lifespan is met
var lifespan = 0 # counted in hours